import { GlobalData } from './global';
import { RequestData } from './request';
import { SessionData } from './session';
export declare type PageData = Record<string, any>;
export declare enum FrontmatterTypes {
    JSON = "json",
    YAML = "yaml"
}
export declare type FrontmatterType = 'json' | 'yaml';
export interface Frontmatter {
    global: GlobalData;
    page: PageData;
    request: RequestData;
    session: SessionData;
}
