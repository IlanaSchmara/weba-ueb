import { Config } from './public/config';
export interface BuzzyInterface {
    start(): Promise<void>;
    stop(): Promise<void>;
}
declare class Buzzy implements BuzzyInterface {
    private logging;
    private config;
    private app;
    constructor(config?: RecursivePartial<Config>);
    start(): Promise<void>;
    stop(): Promise<void>;
}
export default Buzzy;
export { Config, DefaultConfig, Controller, ControllerFunction, EmptyResult, TextResult, JsonResult, RedirectResult, PageResult, FragmentResult, Helper, Route, DefaultMethod } from './public';
