import { FileUtils } from '../filesystem-utils';
import { ReferencedCss } from './types';
import { Request } from '../request';
export declare class CssParser {
    private fileUtils;
    private request;
    constructor(fileUtils: FileUtils, request: Request);
    fetchFromImportRule(path: string, css: string): Promise<ReferencedCss[]>;
    fetchInlineStyles(html: string): Promise<ReferencedCss[]>;
    fetchLinkedStyles(html: string): Promise<ReferencedCss[]>;
    removeStyles(html: string): string;
}
