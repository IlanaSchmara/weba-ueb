import { TemplatingConfig } from '../../public/config';
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
import { Request } from '../request';
import { Validation } from './types';
export { Validation, NuValidation, ValidationResult } from './types';
export declare class Validator {
    private config;
    private logging;
    private fileUtils;
    private request;
    private cssParser;
    private transformer;
    constructor(config: TemplatingConfig, logging: Logging, fileUtils: FileUtils, request: Request);
    private addInlineStyles;
    private injectLinkedCss;
    private _validate;
    validate(html: string): Promise<Validation>;
}
