import { NuValidation, Validation, ValidationResult } from './types';
export declare const DEFAULT_LINE_DELIMITER = "| ";
export declare class TransformValidation {
    private lineDelimiter?;
    constructor(lineDelimiter?: string);
    addLineNumbers(documentLines: string[]): [string[], number];
    determineColumnsToMark(y: number, numerationPad: number, lineLength: number, firstLine: number, lastLine: number, firstColumn: number, lastColumn: number): [number, number];
    map(nuValidation: NuValidation, numerationPad: number, htmlWithLineNumbers: string[], onTransformed: (line: number, transformed: string) => void): ValidationResult;
    transform(nuValidation: NuValidation[], html: string): Validation;
}
