import { RoutingConfig } from '../../public/config';
import { PageRoute } from '../../public/route';
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
import { RenderEngine } from '../render-engine';
import { Validator } from '../validator';
import { Frontmatter } from '../../public/frontmatter';
export declare class HtmlService {
    private config;
    private logging;
    private fileUtils;
    private renderEngine;
    private validator;
    constructor(config: RoutingConfig, logging: Logging, fileUtils: FileUtils, renderEngine: RenderEngine, validator: Validator);
    validate(html: string): Promise<[boolean, string]>;
    private parsePagePath;
    parsePage(ressource: string | PageRoute, frontmatter: Frontmatter): Promise<string>;
    parseTemplate(ressource: string | PageRoute, frontmatter: Frontmatter): Promise<string>;
}
