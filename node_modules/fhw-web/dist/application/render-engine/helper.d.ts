import { TemplatingConfig } from '../../public/config';
import Handlebars = require('handlebars');
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
export declare class HelpersRegistration {
    private config;
    private logging;
    private fileUtils;
    private moduleLoader;
    constructor(config: TemplatingConfig, logging: Logging, fileUtils: FileUtils);
    registerGlobalHelpers(hbs: typeof Handlebars): void;
    private parseCustomHelpers;
    registerCustomHelpers(hbs: typeof Handlebars): Promise<void>;
}
