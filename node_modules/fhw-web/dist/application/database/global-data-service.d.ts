import { GlobalDataConfig } from '../../public/config/database-config';
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
import { SqlDataService } from './sql-data-service';
declare type GlobalData = Record<string, any> | Record<string, any>[];
export declare class GlobalDataService {
    private config;
    private logging;
    private fileUtils;
    private sqlDataService;
    private data;
    constructor(config: GlobalDataConfig, logging: Logging, fileUtils: FileUtils, sqlDataService: SqlDataService);
    private _build;
    build(): Promise<GlobalDataService>;
    reload(): Promise<void>;
    getData(): Promise<GlobalData>;
}
export {};
