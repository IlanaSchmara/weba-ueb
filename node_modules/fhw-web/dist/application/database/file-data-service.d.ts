import { FileDataConfig } from '../../public/config/database-config';
import { JsonData } from '../../public/database';
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
export declare class FileDataService {
    private config;
    private logging;
    private fileUtils;
    private data;
    constructor(config: FileDataConfig, logging: Logging, fileUtils: FileUtils);
    private normalizeFilename;
    loadJson(filename: string): JsonData | null;
    saveJson(filename: string, data: JsonData): void;
    private _build;
    build(): Promise<FileDataService>;
    reload(): Promise<void>;
    save(): Promise<void>;
}
