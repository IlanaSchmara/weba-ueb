import { Config } from '../public/config';
import { LoggingService } from './logging';
import { FileUtils } from './filesystem-utils';
export declare class Application {
    private config;
    private logService;
    private fileUtils;
    private server;
    constructor(config: Config, logService: LoggingService, fileUtils: FileUtils);
    private buildServer;
    private startServer;
    start(): Promise<void>;
    stop(): Promise<void>;
}
