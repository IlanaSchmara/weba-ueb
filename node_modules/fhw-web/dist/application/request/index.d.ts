import { Logging } from '../logging';
import { NuValidation } from '../validator';
import { ServerConfig } from '../../public/config';
export declare class Request {
    private config;
    private logging;
    constructor(config: ServerConfig, logging: Logging);
    sendNuChecker(html: string): Promise<NuValidation[]>;
    getStylesheet(path: string): Promise<string>;
}
