import { RoutingConfig } from '../../public/config';
import { Route } from '../../public/route';
import { Logging } from '../logging';
import { FileUtils } from '../filesystem-utils';
export declare class RoutesParser {
    private config;
    private logging;
    private fileUtils;
    private routesDefinitionFilename;
    constructor(config: RoutingConfig, logging: Logging, fileUtils: FileUtils);
    parseRoutesDefinitionFile(): Promise<Route[]>;
}
