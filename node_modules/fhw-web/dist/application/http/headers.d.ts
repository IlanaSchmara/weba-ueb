import { RequestHeaders } from '../../public/request';
import { Response as ExpResponse } from 'express';
import { IncomingHttpHeaders } from 'http';
export interface Headers extends RequestHeaders {
    'content-type': string;
    referer: string;
    'user-agent': string;
    authorization: string;
    'accept-language': string;
}
export declare const parseHeaders: (headers: IncomingHttpHeaders) => Headers;
export declare const setHeaders: (res: ExpResponse, headers: Record<string, string>) => void;
