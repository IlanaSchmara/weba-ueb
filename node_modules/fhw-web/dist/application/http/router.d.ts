import { Config } from '../../public/config';
import { Express } from 'express';
import { LoggingService } from '../logging';
import { FileUtils } from '../filesystem-utils';
import { ResponseService } from '../response-service';
export declare class Router {
    private config;
    private logService;
    private fileUtils;
    private responseService;
    private logging;
    private app;
    private expRouter;
    constructor(config: Config, logService: LoggingService, fileUtils: FileUtils, responseService: ResponseService);
    private logRequest;
    private logResponse;
    plugIn(app: Express): Promise<void>;
    private setup;
    /**
     * @important must not throw any error
     *
     * @param response FHW-Web-Response Object
     * @param res Express-Response Object
     */
    private sendResponse;
}
